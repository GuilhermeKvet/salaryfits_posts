import 'package:flutter/material.dart';

final salaryFitsTheme = ThemeData(
  fontFamily: 'Aileron',
  colorScheme: ColorScheme.fromSwatch()
      .copyWith(
        primary: const Color(0xfff7cc35),
        secondary: Colors.white,
      )
      .copyWith(background: Colors.white),
);
